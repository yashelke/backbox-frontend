{
  "buildCommand": "cd frontend/my_app && npm install && npm run build",
  "outputDirectory": "frontend/my_app/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
